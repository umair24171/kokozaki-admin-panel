import 'package:flutter/material.dart';

class AccessControls extends StatelessWidget {
  const AccessControls({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
